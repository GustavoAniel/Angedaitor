import { StyleSheet } from "react-native";

const styleModal = StyleSheet.create({
    titulo: {
        fontWeight: 'bold',
        fontSize: 20
    },
    texto: {
        fontSize: 15,
        marginVertical: 5
    }
})

export default styleModal;