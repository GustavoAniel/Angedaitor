import { StyleSheet } from "react-native";

const styleCadastro = StyleSheet.create({
    titulo: {
        fontSize: 30,
        color: 'white',
        textAlign: 'center',
        fontWeight: 'bold'
    }
})

const styleListagem = StyleSheet.create({

})

export {styleCadastro, styleListagem}